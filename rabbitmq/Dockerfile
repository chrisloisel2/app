FROM rabbitmq:4-management

# RabbitMQ avec plugin management web activé.
# Se référer au docker-compose.yml pour la configuration complète.
#
# Ports :
#   5672  — AMQP (producers / consumers)
#   15672 — Web UI management
# Auth  : admin / admin123  (RABBITMQ_DEFAULT_USER/PASS)
# Vhost : /
