FROM jpillora/dnsmasq:latest

# DNS local basé sur dnsmasq avec interface web.
# Se référer au docker-compose.yml pour la configuration complète.
#
# Ports :
#   53/udp — DNS
#   53/tcp — DNS
#   8080   — Web UI (logs)
# Auth web : admin / admin  (HTTP_USER / HTTP_PASS)
# Config  : ./dnsmasq.conf (générer via setup-dns.sh avant de démarrer)
# Requiert : cap_add: NET_ADMIN
